let hora = prompt("QUE HORAS VOÇÊ  ENTROU")

if (hora >= 8 && hora <= 12 || hora >= 13 && hora <= 18) {
    alert("está dentro do horario comercial")

} else {
    alert("Fora do horario comercial")

}